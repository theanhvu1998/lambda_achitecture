# created database:
	CREATE KEYSPACE monitorsystem WITH replication = {'class':'SimpleStrategy', 'replication_factor' : 3};

# real-time tables

created table messen:
	CREATE TABLE messen(
	Messen_id text PRIMARY KEY,
	User_id text,
	Generator_id text,
	C_O2 float,
	C_H2S float,
	T_H2O float,
	T_Machine float,
	T_Evironment float,
	P_Oil float,
	P_Tank float,
	U_AB float,
	I_A float,
	I_B float,
	I_c float,
	Veclovity float,
	Momen float,
	Type smallint,
	Created timestamp,
	);

created table user_active:
	CREATE TABLE user_active(
	User_id text PRIMARY KEY,
	Time timestamp,
	type boolean,
	Created timestamp,
	);

# infor tables

created table user:
	CREATE TABLE user(
	User_id text PRIMARY KEY,
	Generator_id text,
	Name text,
	City smallint,
	Adress text,
	Company text,
	Phone text,
	Mail text,
	Sex boolean,
	Type smallint,
	Created timestamp,
	);

created table generator:
	CREATE TABLE generator(
	Generator_id text PRIMARY KEY,
	User_id text,
	Dateinstall date,
	city smallint,
	size smallint,
	type smallint,
	Created timestamp,
	);

# analytic tables

created table analytic_admin:
	CREATE TABLE analytic_user(
	Analytic_user_id text PRIMARY KEY,
	Total_user smallint,
	User_online smallint,
	User_offline smallint,
	User_disconnect smallint,
	Top_user text,
	Created timestamp,
	);

created table analytic_city:
	CREATE TABLE analytic_city(
	Analytic_city_id text PRIMARY KEY,
	City_id text,
	Total_user smallint,
	Total_time_active int,
	Top_user text,
	Created timestamp,
	);

created table analytic_user:
	CREATE TABLE analytic_user_detail(
	User_id text PRIMARY KEY,
	Rank smallint,
	Total_time_active int,
	Time_active smallint,
	Disconnects_count smallint,
	Created timestamp,
	);

created table detect_false:
	CREATE TABLE detect_false(
	Detect_false_id text PRIMARY KEY,
	User_ID text,
	False_detail text,
	Created timestamp, 
	);

# defaul tables

created table city:
	CREATE TABLE city(
	City_id smallint PRIMARY KEY,
	City_amchart_id text,
	Name text,
	Side smallint,
	);

created table date:
	CREATE TABLE date(
	Date_id smallint PRIMARY KEY,
	Date_name text,
	Day smallint,
	Holiday text,
	Week smallint,
	Month smallint,
	season text,
	Quarter smallint,
	Hal_year smallint,
	Year Smallint,
	);

