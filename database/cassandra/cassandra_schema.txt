float = number
text = string
boolean = boolean

# created database:
	CREATE KEYSPACE monitorsystem WITH replication = {'class':'SimpleStrategy', 'replication_factor' : 1};

===============================================================================================================================================
# real-time tables



created table messenger:
	CREATE TABLE messenger(
	msgid text PRIMARY KEY,
	sessionid text,
	rpi text,
	time bigint,
	Type text,
	data list<frozen<map<text,text>>>,
	);

created table status:
	CREATE TABLE status(
	msgid text PRIMARY KEY,
	rpi text,
	userid text,
	time bigint,
	status smallint,
	messenger text,
	);

# infor tables

created table user:
	CREATE TABLE user(
	msgid text PRIMARY KEY,
	generator_id text,
	name text,
	city smallint,
	adress text,
	company text,
	phone text,
	mail text,
	sex boolean,
	type smallint,
	time bigint,
	);

created table generator:
	CREATE TABLE generator(
	msgid text PRIMARY KEY,
	userid text,
	insatll bigint,
	city smallint,
	size smallint,
	type smallint,
	time bigint,
	);

# analytic tables

created table analytic_admin:
	CREATE TABLE analyticadmin(
	id text PRIMARY KEY,
	Total_user smallint,
	Start bigint,
	Stop bigint,
	Disconnect smallint,
	Top_user text,
	time bigint,
	);

created table analytic_city:
	CREATE TABLE analyticcity(
	id text PRIMARY KEY,
	City_id text,
	Total_user smallint,
	Total_time_active int,
	Top_user text,
	time bigint,
	);

created table analytic_user:
	CREATE TABLE analyticuser(
	id text PRIMARY KEY,
	User_id text,
	Rank smallint,
	Total_time_active int,
	Disconnects_count smallint,
	time bigint,
	);

created table detectfalse:
	CREATE TABLE detectfalse(
	id text PRIMARY KEY,
	User_ID text,
	Generator_id text,
	False_detail text,
	time bigint, 
	);

created table ml:
	CREATE TABLE ml(
	id text PRIMARY KEY,
	g01eleia float,
	g01eleib float,
	g01eleic float,
	g01elevab float,
	g01eles float,
	g01elef float,
	);

# defaul tables

created table city:
	CREATE TABLE city(
	id smallint PRIMARY KEY,
	amcahrtid text,
	name text,
	name_eng text,
	type text,
	type_eng text,
	);

created table date:
	CREATE TABLE date(
	Date_id smallint PRIMARY KEY,
	Date_name text,
	Day smallint,
	Holiday text,
	Week smallint,
	Month smallint,
	season text,
	Quarter smallint,
	Hal_year smallint,
	Year Smallint,
	);

